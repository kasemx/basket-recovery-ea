# R-3.1 Gap Analysis — Strategy Command Wiring

Baseline: `bc989629` / `v0.4.1-compile-stable`  
Scope: command wiring, controlled migration, runtime event handlers, persistence round-trip, scheduler — **no Trade Executor / broker API**.

---

## Capability matrix

| # | Capability | Status | Evidence / notes |
|---|------------|--------|------------------|
| 1 | `EvaluateStrategyCommand` registered with `CommandDispatcher` | **Already complete** | `KernelHandlerRegistration.mqh` priority 40; wired in `ApplicationKernel.mqh` |
| 2 | `EvaluateStrategyCommandHandler` validates version, hash, migration, idempotency | **Already complete** | `StrategyCommandSupport` + `CBasketRuntimeGuard`; idempotency via `CommandProcessor` + `IIdempotencyStore` |
| 3 | Evaluation builds context → engine → mapper → enqueue | **Already complete** | `EvaluateBasketStrategyUseCase.mqh`; no recursive `RunCycle` inside handler |
| 4 | Mapped commands persisted and survive restart | **Already complete** | `PersistentCommandQueue` + `TestStrategyCommandRestartRecovery` (7 types) |
| 5 | Strategy command serializer/deserializer | **Already complete** | `CommandSerializerStrategy.mqh` — 7 types, common + payload fields |
| 6 | Strategy events update permitted runtime state only | **Already complete** | `StrategyRuntimeEventHandlers*.mqh`; duplicate PL/BE rejected on aggregate |
| 7 | Execution-intent handlers = safe stub / pending event | **Already complete** | `StrategyExecutionStubHandlers.mqh` (open/close/move/reduce); `DisableRecovery` / `MarkProfitLevelCompleted` are **runtime-state** handlers (not broker execution) |
| 8 | Scheduler evaluates ACTIVE baskets only | **Already complete** | `StrategyEvaluationScheduler.mqh` filters `BRE_STATE_ACTIVE`, bound profile, `!StrategyMigrationRequired()` |
| 9 | Timer order bounded and deterministic | **Already complete** | `ApplicationTimerPipeline.mqh`: REST → `RunCycle` → `FlushIfDue` → scheduler; `OnTick` empty in EA |
| 10 | Controlled migration via explicit action only | **Already complete** | `BindMigratedBasketStrategyUseCase.mqh`; rejects non-migration / already-bound; explicit JSON + profile required |

---

## Classification detail

### Already complete (no code change required)

- Full handler registration map (10 command + 8 event handlers)
- Evaluate flow with audit + basket save
- Command persistence fields: `basketId`, `expectedBasketVersion`, `strategyProfileHash`, `idempotencyKey`, `correlationKey`, payload
- Migration audit via `BumpVersion` inside `CompleteStrategyMigration`
- Application layer contains **zero** `OrderSend` / `OrderModify` / `PositionClose` / `PositionSelect` / `HistorySelect` references

### Incomplete → closed in this sprint pass

| Item | Was | Action |
|------|-----|--------|
| Handler registration test coverage | Missing explicit test | Added `TestHandlerRegistrationMap` |
| All execution stub handlers tested | Only close-positions covered | Added `TestAllExecutionStubHandlersProducePendingEvent` |
| Migration reject already-bound | Partial (evaluate path only) | Added `TestMigrationRejectsAlreadyBoundBasket` |

### Incorrect

None identified against R-3.1 specification.

### Missing (deferred — out of R-3.1 scope)

| Item | Rationale |
|------|-----------|
| Trade execution (`OrderSend`, etc.) | Explicitly excluded; stubs emit `BRE_EVENT_EXECUTION_PENDING` |
| `CStrategyProfileBoundEventHandler` state mutation | Binding applied in `BindMigratedBasketStrategyUseCase` before event emission; handler no-op is intentional |
| Live market/risk inputs in evaluate handler | `adverseMovePips` / `floatingProfitUsd` passed as `0.0` — placeholder for later sprint |

---

## Handler registration map (command)

| Priority | Handler | Command type |
|---------:|---------|--------------|
| 10 | `CCreateBasketCommandHandler` | `BRE_COMMAND_CREATE_BASKET` |
| 20 | `CActivateBasketCommandHandler` | `BRE_COMMAND_ACTIVATE_BASKET` |
| 30 | `CCloseBasketCommandHandler` | `BRE_COMMAND_CLOSE_BASKET` |
| 40 | `CEvaluateStrategyCommandHandler` | `BRE_COMMAND_EVALUATE_STRATEGY` |
| 50 | `COpenRecoveryPositionCommandHandler` | `BRE_COMMAND_OPEN_RECOVERY_POSITION` |
| 50 | `CClosePositionsCommandHandler` | `BRE_COMMAND_CLOSE_POSITIONS` |
| 50 | `CMoveBasketStopLossCommandHandler` | `BRE_COMMAND_MOVE_BASKET_STOP_LOSS` |
| 50 | `CReduceBasketRiskCommandHandler` | `BRE_COMMAND_REDUCE_BASKET_RISK` |
| 60 | `CDisableRecoveryCommandHandler` | `BRE_COMMAND_DISABLE_RECOVERY` |
| 60 | `CMarkProfitLevelCompletedCommandHandler` | `BRE_COMMAND_MARK_PROFIT_LEVEL_COMPLETED` |

## Event handler map

| Handler | Event |
|---------|-------|
| `CProfitLevelReachedEventHandler` | `BRE_EVENT_PROFIT_LEVEL_REACHED` |
| `CProfitLevelCloseRequestedEventHandler` | `BRE_EVENT_PROFIT_LEVEL_CLOSE_REQUESTED` |
| `CProfitLevelCloseCompletedEventHandler` | `BRE_EVENT_PROFIT_LEVEL_CLOSE_COMPLETED` |
| `CBreakEvenActivatedEventHandler` | `BRE_EVENT_BREAK_EVEN_ACTIVATED` |
| `CRecoveryDisabledEventHandler` | `BRE_EVENT_RECOVERY_DISABLED` |
| `CRiskReductionRequestedEventHandler` | `BRE_EVENT_RISK_REDUCTION_REQUESTED` |
| `CBasketLockedEventHandler` | `BRE_EVENT_BASKET_LOCKED` |
| `CStrategyProfileBoundEventHandler` | `BRE_EVENT_STRATEGY_PROFILE_BOUND` (no-op; state already committed) |

---

## Persistence coverage (strategy commands)

| Field | Serialized | Restored |
|-------|------------|----------|
| `command_type` | Yes | Yes |
| `command_id` | Yes | Yes |
| `idempotency_key` | Yes | Yes |
| `basket_id` | Yes | Yes |
| `correlation_key` | Yes | Yes |
| `expected_basket_version` | Yes | Yes |
| `strategy_profile_hash` | Yes | Yes |
| Type-specific payload | Yes | Yes |

---

## Test coverage (R-3.1)

| Scenario | Test function |
|----------|---------------|
| Serialization round-trip | `TestStrategyCommandSerializationRoundTrip` |
| Restart recovery | `TestStrategyCommandRestartRecovery` |
| Evaluate dispatch | `TestEvaluateStrategyDispatchAndEnqueue` |
| Stale version | `TestEvaluateRejectsStaleVersion` |
| Hash mismatch | `TestEvaluateRejectsHashMismatch` |
| Migration required | `TestEvaluateRejectsMigrationRequired` |
| Explicit migration success | `TestControlledMigrationSuccess` |
| Duplicate profit level | `TestDuplicateProfitLevelReachedRejection` |
| Duplicate break-even | `TestDuplicateBreakEvenRuleRejection` |
| Execution stub pending | `TestExecutionStubProducesPendingEvent` |
| All execution stubs | `TestAllExecutionStubHandlersProducePendingEvent` |
| ACTIVE-only scheduler | `TestActiveOnlyStrategyEvaluationScheduling` |
| Timer ordering | `TestTimerPipelineOrdering` |
| Handler registration | `TestHandlerRegistrationMap` |
| Migration reject bound | `TestMigrationRejectsAlreadyBoundBasket` |

---

## Scheduler order (production)

```text
1. REST ingestion if due     (CommandIngestionService.PollAndEnqueue)
2. CommandProcessor.RunCycle
3. PersistenceManager.FlushIfDue
4. StrategyEvaluationScheduler.RunIfDue
```

`OnTick()` in `BasketRecoveryEA.mq5` remains empty.
